# PythonPhysicsGoBRRRR
This is my awful code, I am working on a good version dw please don't come roast me and tell me to use semi-implicit euler i lit don't know physics.

Since I made this public and GPL maybe you smart github people will fix my code for me and make it good.




--- MAKING IT WORK ---
make sure u have python installed:
sudo pacman -S python python-pip
(IF YOU USE DEBIAN OR ANOTHER UNWORTHY DISTRO THEN GO FIGURE IT OUT) (winget install python for windows)

make a python virtual environment in the same directory as the code 'main.py'.
python -m venv venv

run source venv/bin/activate for bash, and for fish shell use source venv/bin/activate.fish (also get a posix compliant shell). If you are on windows powershell/terminal (whats the difference \: ?), i think its ./venv/Scripts/activate but search it up or smth.
If you use a random shell, then figure it out.

Install the requirements, make sure to have the requirements.txt in the same directory. Run this: pip install -r requirements.txt    OR   js do pip install pygame

run the main.py file now with python main.py
